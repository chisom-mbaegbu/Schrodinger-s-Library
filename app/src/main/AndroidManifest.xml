<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="comp3350.schrodingers">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.PurchaseHistoryActivity"></activity>
        <activity
            android:name=".presentation.SearchActivity"
            android:label="@string/title_activity_search"
            android:parentActivityName=".presentation.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="comp3350.schrodingers.presentation.HomeActivity" />
            <intent-filter>
                <action android:name="android.intent.action." />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.HomeActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ViewBookInfoActivity"/>
        <activity android:name=".presentation.LoggedActivity" />
        <activity android:name=".presentation.PersonInfo" />
        <activity android:name=".presentation.PaymentActivity" />
        <activity android:name=".presentation.CreateAccountActivity" />
        <activity android:name=".presentation.LoginActivity" />
        <activity android:name=".presentation.NotLoggedActivity" />
        <activity android:name=".presentation.WishlistActivity"/>
        <activity android:name=".presentation.ReviewPurchaseActivity"/>
        <activity android:name=".presentation.ShoppingCartActivity"/>
        <activity android:name=".presentation.RecommendationsActivity"/>
        <activity android:name=".presentation.OrderCompletedActivity"/>
    </application>

</manifest>